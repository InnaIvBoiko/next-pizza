// Pizza-themed fallback icon shown when a product has no image URL. PNG (not
// SVG) so next/image serves it without needing `dangerouslyAllowSVG`. The SVG
// source lives alongside it for future edits.
export const PRODUCT_IMAGE_PLACEHOLDER = '/product-placeholder.png';
