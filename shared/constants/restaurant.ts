// The pizzeria operates in Italy, so order timestamps are always shown in its
// local time — independent of the server timezone (UTC on most hosts) or where
// the visitor happens to be.
export const RESTAURANT_TIME_ZONE = 'Europe/Rome';
